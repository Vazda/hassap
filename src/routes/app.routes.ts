import {
    Router,
  } from 'express';
  
  import app from '../controllers/app.controller';
  
  const router = Router();
  
  //  router.route('/')
  //    .get(app.testRoute);
  
  //  router.route('/user')
  //    .post(app.addUser);
  
  export default router;


  